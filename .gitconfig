[user]
	email = KeyboardInterrupt@KeyboardInterrupt.com
	name = KeyboardInterrupt

[push]
	default = simple

[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
        required = true
        clean = git-lfs clean -- %f
